type Post{
    id: ID
    title: String!
    body: String!
    author: User!
}
input PostInput{
    title: String!
    body: String!
    author: ID!
}
type Query{
    posts: [Post!]!
}
type Mutation{
    createPost(post: PostInput): Post
}